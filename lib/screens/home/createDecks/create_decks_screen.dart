import 'package:flutter/material.dart';

class CreateDecksScreen extends StatelessWidget {
  const CreateDecksScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("create decks page"),
      ),
    );
  }
}
